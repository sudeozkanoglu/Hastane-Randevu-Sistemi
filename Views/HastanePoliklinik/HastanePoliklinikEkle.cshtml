@model webProjeOdev8.Models.HastanePoliklinik

@{
    ViewData["Title"] = "HastanePoliklinikEkle";
}

<h1>HastanePoliklinikEkle</h1>

<h4>HastanePoliklinik</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="HastanePoliklinikEkle">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="hastaneId" class="control-label">Hastane</label>
                <select asp-for="hastaneId" class="form-control" asp-items="@ViewBag.HastaneList">
                    <option value="">Seçiniz...</option>
                </select>
            </div>
            <div class="form-group">
                <label asp-for="Poliklinik.poliklinikAdi" class="control-label"></label>
                <select asp-for="poliklinikId" class="form-control" asp-items="@ViewBag.PoliklinikList">
                    <option value="">Seçiniz...</option>
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
