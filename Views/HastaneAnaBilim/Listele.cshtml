@model IEnumerable<webProjeOdev8.Models.HastaneAnaBilim>

@{
    ViewData["Title"] = "Listele";
}

<h1>Listele</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Hastane)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AnaBilimDali)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Hastane.hastaneAdi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AnaBilimDali.anaBilimDaliAdi)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Sil", new {  id1=item.Hastane.hastaneId, id2=item.AnaBilimDali.anaBilimDaliId  })
            </td>
        </tr>
}
    </tbody>
</table>
